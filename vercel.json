{
  "buildCommand": "cd frontend && npm run build",
  "outputDirectory": "frontend/dist",
  "devCommand": "cd frontend && npm run dev",
  "installCommand": "cd frontend && npm ci || npm install",
  "framework": "vite",
  "functions": {
    "api/*.py": {
      "runtime": "@vercel/python"
    }
  }
}

